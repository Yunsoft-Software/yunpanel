# Restic izole Ubuntu kabulü — 2026-09-20

Kapsam: yalnız `.local/test-server.env` içindeki `157.180.11.28` YunPanel test sunucusu. `.44` Plesk sunucusuna bağlantı kurulmadı. GitHub `origin/main` ve yerel `main` aynı `3f6bcd0a` commit'indeydi; diğer branch'lerde main'e göre benzersiz yeni commit yoktu.

Kaynak incelemesinde API'nin `createWebsiteHttpHealthInspector` barrel import'u ve opsiyonel `databaseBindingRegistry` olmadan kurulan backup/restore route'u API başlangıcını kırıyordu. Import subpath'e taşındı, backup/restore route'u gerekli kayıtçı varsa bağlandı. Website restore prototipi ayrıca gerçek restic adapter'ına `repoPath`/`targetPaths`/`excludePatterns` gönderirken adapter `repository`/`paths`/`excludes` bekliyordu; çağrı sözleşmesi ve testleri düzeltildi. Restic parolası `RESTIC_PASSWORD` environment yerine `0700` geçici dizindeki `0600` dosya üzerinden `RESTIC_PASSWORD_FILE` ile geçiriliyor; normal dönüş/hata sonrası dosya kaldırılıyor ve CLI hata mesajındaki exact parola maskeleniyor.

Gerçek restore hâlâ durable job, resource lock ve restart-safe evidence taşımadığı için varsayılan authenticated API mutasyonu `website_restore_not_ready` (503) ile fail-closed tutuldu. Preview/test prototipi source'da kalır; production restore tamamlanmış sayılmaz. Gerekli kod işi `plan.md` P1.2, gerçek Website kabulü `todo.md` T-BACKUP altındadır.

`.28` üzerinde Ubuntu 24.04 paket deposundan yalnız `restic 0.16.4-2ubuntu0.24.04.3` kuruldu; apt simülasyonu `0 upgraded, 1 newly installed, 0 to remove` gösterdi. `rclone` kurulmadı. Mevcut YunPanel paketi `0.3.0-2026092006` olarak kaldı; bu turdaki kaynak değişiklikleri canlı panele deploy edilmedi. `yunpanel-api` ve `yunpanel-web` servisleri kurulum sonrası `active` idi. Loopback HTTPS health isteği IP allowlist tarafından reddedildiği için bu istek uygulama health kabulü sayılmadı.

Yeni restic manager kaynak dosyası ve izole test sürücüsü geçici olarak test sunucusuna taşındı. Sürücü `/var/tmp` altında rastgele fixture/depo oluşturdu, gerçek binary ile `init`, `check`, `unlock`, iki `backup`, tag'li `list`, `forget --keep-last 1`, `prune`, yeniden `check` ve `restore` çağırdı. İki snapshot'tan birinin kaldığı, restore edilen dosyanın beklenen `snapshot-v2` içeriğine sahip olduğu, parola dosyası/dizininin `0600/0700` olduğu, parolanın CLI argümanına veya child environment'a konmadığı ve her çağrı sonrası parola dosyasının silindiği assertion'larla doğrulandı. Test çıktısı `status: ok` idi. Fixture ve staging dosyaları temizlendi; `/var/tmp` altında bu teste ait dizin kalmadı. Parola değeri kayda alınmadı.

Bu kanıt gerçek Website backup set'i, DB/Maildir hooks, lock contention, process-kill secret cleanup, panel job/audit, package upgrade, rclone remote veya restore rollback kabulü değildir. Bunlar `todo.md` içinde açık kalır.
