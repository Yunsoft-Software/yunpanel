# Plesk görev düzeni — sade reseller kapsamı

2026-09-23; `development`. Kullanıcının son kararı önceki tam reseller/paket/abonelik zorunluluğunu daraltır. Plesk'e benzeyen kullanım düzeni, mevcut site araçları ve güvenlik sınırları korunur. Bu belge `plan.md` PAR-01/02/03 ve UX-PL-02 için güncel reseller sözleşmesidir; eski envanterin geniş reseller satırları ilk sürümün kabul şartı değildir.

## İlk sürümde kullanıcı ne yapacak?

| Hesap | Görünen işler | Sınır |
| --- | --- | --- |
| Owner | Mevcut sunucu/site araçları; bayiler ve müşteriler; basit adet limitleri | Tüm yetkili yerel kaynaklar; son aktif Owner korunur |
| Reseller | Müşterilerim, Sitelerim, Hesabım; müşteri ekleme/düzenleme, kendi kapsamındaki site araçları | Yalnız kendine bağlı müşteriler/siteler; sunucu ayarları, root terminal, başka bayi ve Owner hesapları yok |
| Customer | Web Siteleri ve Alan Adları, Posta, Dosyalar, Veritabanları ve diğer izinli mevcut site araçları | Yalnız kendi siteleri; başka müşteri veya kullanıcı yönetim yetkisi yok |

Ayrı bir reseller dashboard, tema, paket editörü veya abonelik ekranı gerekmez. Mevcut liste/form/site araçları kullanılır. Yönetici Service Provider/Power User ayrımı uzun vadeli UX referansıdır; sade reseller için iki yeni panel inşa etmek önkoşul değildir.

## Küçük veri modeli

**Owner → isteğe bağlı tek Reseller → Customer → mevcut Website.** Müşteri doğrudan Owner'a bağlı olabilir (`resellerId: null`). Reseller altında reseller kurulmaz. Reseller kendi sitesini yönetecekse normal müşteri kaydı üzerinden aynı akış kullanılır; ayrı bir hosting aboneliği türü açılmaz.

Mevcut auth kullanıcıları ve Website kimlikleri tekrar oluşturulmaz. Müşteri sahipliği, site erişim üyeliği ve login rolü farklı kavramlardır: mevcut `site_manager` otomatik olarak reseller veya müşteri sahibi sayılmaz. Veri bağlantısı açık, sürümlü ve geri alınabilir olmalıdır; Unix kullanıcıları aynı kalır. Girdide gelen actor rolü veya sahiplik alanı tek başına yetki değildir.

Kaynak politika sözleşmesi: trusted actor `{id, role, active}`; hesap profili `{id, kind, resellerId, active}`; Website sahiplik projeksiyonu `{id, customerId}`. Profil `id` mevcut auth kullanıcı kimliğiyle bağlanmalıdır; ayrı kopya login deposu kurulmaz. `kind` reseller/customer ayrımıdır; reseller'ın `resellerId` değeri açık `null` olur. Bunlar HTTP body şeması değildir. RS-02a/b/c ile aynı auth DB'sinde sürümlü profil/limit deposu ve kullanıcı deposu bağlantısı kaynakta uygulandı. RS-02d, buna kalıcı site kontenjanı ve mevcut site-create motoruna iç servis bileşimi ekledi; canlı veritabanında migration veya site HTTP/job runtime bağlantısı çalıştırılmadı. Mevcut Website kaydı sadece bu yardımcı için yeniden oluşturulmaz; eski site üyelikleri otomatik sahipliğe çevrilmez.

**Basit limit:** Owner'ın belirlediği toplam müşteri ve Website adedi. `null` sınırsız, `0` yeni kayıt açılamaz; negatif, kesirli veya bilinmeyen kullanım geçersizdir. İki limit de açık verilmelidir; eksik limit otomatik sınırsız değildir. Askıdaki kayıtlar da adet tüketir. Sınır düşürmek mevcut veriyi silmez, yeni eklemeyi engeller. Site limiti bayinin tüm müşterileri toplamıdır. Kontrol ve kayıt aynı işlem/kilit içinde yapılır; salt önizleme kota enforcement değildir. Disk/trafik/CPU/RAM için ikinci bir reseller ölçüm veya rezervasyon motoru kurulmaz; mevcut site ölçüm/limit işleri PROD-15'te sürer.

RS-02d'nin site kontenjanı ikinci bir provisioning motoru değildir: mevcut oluşturma başlamadan auth transaction'ında `reserved`, registry snapshot'ı doğrulanıp sahiplik yazılınca `attached` olur. Her ikisi bir kontenjan tüketir, aynı işlem ikinci kez sayılmaz. Hata/timeout sonrasında kayıt otomatik temizlenmez; mevcut lifecycle ile güvenli cleanup ve yeniden yazmanın engellendiği kanıtlanmadan release açılmaz. `attached` çalışır site, host kabulü veya site erişim yetkisi değildir. Auth SQLite ile Website registry/host arasında dağıtık transaction kurulduğu iddia edilmez.

## İlk sürümde yapılmayacaklar

Alt bayi, reseller hizmet paketleri, add-on, zorunlu Subscription, paket sync/lock/unsynced/customization, overselling, otomatik expiry/faturalama, marka özelleştirme, müşteri↔reseller dönüşümü, toplu transfer ve login-as sonraki fazdır. İlk sürümde sahiplik aktarımı API'den de kapalı tutulur; yalnız formdan kaldırmak yeterli değildir. Mevcut site askıya alma/silme motorları korunur; hesap askıya alma ile hosttaki siteyi kapatma aynı işlem gibi gösterilmez.

## Küçük uygulama dilimleri

- [x] **RS-00 — Kapsam sadeleştirildi.** `agents.md`, `plan.md`, `ui-plan.md`, bu sözleşme ve faz ayrımlı özellik envanteri güncel kararla uyumlu. Geniş envanterin tamamı ayrıca korundu. Doküman işidir, çalışan reseller değildir.
- [x] **RS-01 — Kaynak politika çekirdeği.** Saf ve durum tutmayan kapsam/sayım/adet kontrolleri yazıldı ve test edildi; HTTP/persistence yetkisi yerine geçmez.
  - [x] **RS-01a:** `apps/api/src/reseller-scope.js`, `cd89f8d8`. Tek bayi seviyesi, aktif actor/üst hesap, açık müşteri–Website bağı, Owner yönetimi/onarımı, başka bayi/müşteri reddi. `reseller-scope.test.js`: **41 geçti**. [Kaynak raporu](../history/reseller-scope-source-2026-09-23.md).
  - [x] **RS-01b:** `apps/api/src/reseller-limits.js`, `87a23c2a`. İki açık adet limiti; tam snapshot'tan askıdaki kayıtları da sayma; duplicate/orphan/bilinmeyen veriyi reddetme; ekleme kapasitesi. `reseller-limits.test.js`: **66 geçti**. İki dosya birlikte **107 geçti / 0 başarısız**, Node22.16.0; syntax kontrolleri geçti. [Limit raporu](../history/reseller-limits-source-2026-09-23.md).
- [ ] **RS-02 — Mevcut auth/state entegrasyonu.** Mevcut kullanıcı/Website depolarına küçük ve sürümlü ilişki katmanı; idempotent migration, rollback, eşzamanlı adet kontrolü. Rolü açmadan bütün API/list/job/AI/tool/gateway/WS hedeflerini canlı ilişki üzerinden doğrula. Askıya alma/ilişki değişiminde oturum ve açık erişimleri iptal et. Politika kayıtlarını request'ten değil güncel depodan üret.
  - [x] **RS-02a kaynak:** aynı auth SQLite içinde sürümlü ilişki şeması, mevcut kayıtlara dokunmayan idempotent kurulum, tek seviye/sahiplik korumaları ve yalnız boş şemada rollback; `ca40a2ae`, 21 SQLite testi.
  - [x] **RS-02b kaynak:** Owner kontrollü mevcut login–profil bağlantısı, kalıcı liste/limitler, aynı transaction içinde müşteri kapasitesi + kayıt + audit, revizyon ve oturum iptali; `524b0627`. İki bağımsız SQLite bağlantısının son müşteri kontenjanına yarışı dahil 55 test.
  - [x] **RS-02c kaynak/fixture:** `authStore.users.hostingAccounts` mevcut kullanıcı deposuna bağlandı; genel kullanıcı API'sinin profilli hesapta rol/active/site grant/silme yolunu atlaması engellendi. `e1bd0edb`: 5 yeni entegrasyon + 16 mevcut kullanıcı regresyon testi. Native auth testi Node22'de atlandı; gerçek auth kabulü tamamlandı değildir.
  - [x] **RS-02d.1 kaynak:** auth DB'sinde sürümlü kalıcı site kontenjanı, immutable işlem/müşteri/Website bağı ve boş rollback; `5b2afce3`, 6 SQLite testi.
  - [x] **RS-02d.2 kaynak:** `hostingAccounts.siteAllocations`, atomik kontenjan/rezervasyon/audit, doğrulanmış sahiplik, oturum iptali, çift sayım ve audit rollback koruması; `99473414`, 39 depo ve 2 bağımsız SQLite yarış testi.
  - [x] **RS-02d.3 kaynak/fixture:** `hosting-site-create-service.js` ve mevcut isolated site engine'i kullanan runtime factory; müşteri/plan bağlı onay, input snapshot, await sonrasında güncel yetki, registry sonucu doğrulama, aynı servis örneğinde tekrar sıralama; `654dbf64`, 18 servis/kaynak testi. Bu turun toplamı **65 geçti / 0 başarısız / 0 atlandı**, Node22.16.0. [Kaynak raporu ve sınırlar](../history/reseller-site-allocation-2026-09-23.md). Factory gerçek engine ile çalıştırılmadı; HTTP/host kabulü değildir.
  - [ ] **RS-02e / kalan:** HTTP/job runtime bağlantısı; bütün mevcut Website/provisioning/removal yazıcılarında kaynak kilidi ve canlı API/job/AI/tool/gateway/WS tenant kapsamı; güvenli silme/compensation sonrasında kontenjan release; hesap suspend/revoke; veri içeren migration/rollback; Node24/npm11/native auth ve gerçek host/browser kabulü. Önceki auth test sonuçları ayrı [rapordadır](../history/reseller-auth-storage-2026-09-23.md); eski 97/107 testler bu tur yeniden çalıştırılmadı.
- [ ] **RS-03 — Basit hesap API'si.** Owner bayi/müşteri oluşturur ve adet sınırı verir; bayi yalnız kendi müşterisini ekler/düzenler. Kimlik/parent/rol mass-assignment engeli, sunucu tarafında liste filtreleme, audit ve revizyon çakışması. Bağlı müşterisi/sitesi olan hesabı sessiz cascade-delete etme; açık engel ve çözüm göster.
  - [x] **RS-03a kaynak/HTTP:** Owner profil listeleme/bağlama/limit/boş profil kaldırma `/api/users/hosting/accounts` üzerinden mevcut kullanıcı yönetimine bağlandı; `fc4672df` / `cca9745b`. Önceki tur 83 test geçti; rapor `062c3151` ile güncellendi. [API sözleşmesi](../history/hosting-owner-api-2026-09-23.md). Yeni login veya reseller self-service değildir.
- [ ] **RS-04 — Mevcut UI'yi bağla.** Owner için Bayiler/Müşteriler, bayi için Müşterilerim/Sitelerim; tek basit hesap formu, mevcut site araçları. Paket/abonelik seçtirme, boş buton ekleme, Files girişlerini kaldırma. Kullanıcıya yalnız gerçekten çalışan limitleri göster.
  - [x] **RS-04a.1 kaynak:** genel kullanıcı formu delta PATCH; yalnız isim değişiminde değişmeyen rol/active/site grant gönderilmez, backend koruması korunur; `058433a4`, 15 yeni + 12 mevcut istemci testi.
  - [x] **RS-04a.2 kaynak:** doğrulanmış profil istemcisi, açık limitler, kimlik/parent/sonuç kontrolü, ayrı okuma kanalları ve eski oturum/yanıt reddi; belirsiz yazma otomatik tekrarlanmaz; `ecd39609`, 49 istemci/model testi.
  - [x] **RS-04a.3 kaynak:** mevcut Owner `/settings/users` içinde Bayiler/Müşteriler listesi ve ortak profil penceresi; kullanıcı satırından profil bağlama, bayi seçimi, limit ve boş profil kaldırma; `c7eed21e`, 6 kaynak bağlantısı testi. Yeni dashboard/tema/rol/site yetkisi yok.
  - [ ] **RS-04a gerçek kabul:** Node24/npm11, gerçek React/Vite build ve backend ile browser; mobil, klavye, 26+ kayıt, 401/403/409/500, kayıp cevap ve oturum değişimi. Bu turun kaynak toplamı **82 geçti / 0 başarısız / 0 atlandı**, Node22.16.0; render/host testi değildir. [Kaynak raporu](../history/hosting-owner-ui-2026-09-23.md), TODO T-DEV-OWNER-PROFILES.
- [ ] **RS-05 — Gerçek kabul.** Owner, iki bayi, her bayide iki müşteri ve doğrudan Owner müşterisi. Başka tenant kimliğiyle API/job/Files/DB/AI/WS denemeleri; yetki iptali; paralel limit yarışları; restart ve migration rollback; gerçek tarayıcı. Hedef Node24/npm11 tam check ayrı çalıştırılır.

## Kapanış sınırı

İlk sürüm RS-01–05 ile kapanır; gelişmiş reseller özellikleri beklenmez. Reseller dışındaki site/OS/premium yol haritası iptal edilmez. **RS-00/01, RS-02a/b/c/d, Owner API RS-03a ve Owner UI RS-04a.1–3 kaynak alt işleri tamamlandı. RS-02e, reseller/customer self-service, RS-03–05 üst işleri ve gerçek kabul açıktır.** Owner profillerine API/UI girişi kaynakta vardır; yeni reseller login rolü veya site erişimi açılmadı. İç hesap deposu `stage: profile_only`, site köprüsü `stage: ownership_recorded`, `accessGranted: false` ve `provisioningReady: false` döndürür. Kayıtlı/ayrılmış siteler `registered_ownership` veya `registered_and_reserved_ownership` kapsamıyla sayılır; bütün canlı host sitelerinin envanteri değildir. Eski site_manager ilişkileri otomatik taşınmaz; profil bağlanan hesapların genel rol/active/site grant değişikliği geçiş tamamlanana kadar kapalıdır. Servis içi Map bütün süreçler için kaynak kilidi yerine geçmez. Kaynak testi geçti diye production-ready denmez. Gerçek ortam işleri `development-todo.md` ve kök `todo.md` içinde açık kalır.