# YunPanel — Database Gerçek Ortam Kabul TODO

Bu dosya `plan.md` F. Veritabanı bölümündeki kaynak geliştirmelerin gerçek Ubuntu/MySQL/MariaDB kabul kapılarını tutar. Buradaki maddeler geçmeden veritabanı modülü production-ready sayılmaz. GitHub Actions kullanma.

- [ ] MariaDB↔MySQL conflict fail-closed kalsın; mevcut engine otomatik değiştirilmesin. Unix-socket root auth ile engine/version/non-system inventory ve create→inspect→drop→inspect akışı gerçek hostta doğrulansın; system DB ve injection isimleri reddedilsin.
- [ ] Database ownership binding gerçek packaged Owner API'de doğrulansın: schema yalnız aynı server Website/Application ve exact `yunapp-*` site user'a bağlanabilsin; proxy/Docker/cross-server/stale binding reddedilsin, binding varken schema DROP engellensin ve restart/upgrade binding state'i korusun.
- [ ] DB credential lifecycle gerçek hostta doğrulansın: credential create/grant/rotation control-plane desired-state olarak kalsın; explicit apply sonrası yalnız deterministik `ydb_*@localhost` account oluşsun ve yalnız hedef schema için allowlist'li grantler verilsin. Marker olmayan isim çakışması ve global/cross-schema/table/routine grant drift'i mutation üretmeden conflict versin.
- [ ] Credential rotation gerçek bağlantı testiyle doğrulansın: yeni credential ile local DB connection başarılı, önceki credential rotation sonrası başarısız olmalı. Apply/delete failure eski account+grant snapshot'ına dönmeli. Lost-ack durumda `recover-database-credential` current revision/digest + private receipt + exact marker/account/grant live evidence olmadan running job'ı kapatmamalı. Credential delete terminal job'u olmadan encrypted registry finalize edilmemeli; credential varken unbind engellenmeli.
- [ ] `database.backup` gerçek schema üzerinde doğrulansın: exact `backup:<databaseName>` confirmation ile durable job açılsın ve job ID private backup ID olsun. `/var/lib/yunpanel/backups/databases` ile backup directory `0700`, `dump.sql` ve `manifest.json` `0600` kalsın. Dump canonical socket-only `--single-transaction --skip-dump-date --order-by-primary --databases` sözleşmesini kullansın; child process minimal locale/PATH ile çalışsın.
- [ ] Database backup public API/job/result yalnız engine/version/SHA-256/byte/timestamp metadata'sı göstersin; absolute path, SQL içeriği ve child-process raw output görünmesin. Tamper, permission drift, empty dump, başka schema için aynı backup ID ve missing schema fail-closed olsun. Durable completion öncesi kesintide `recover-database-backup` yalnız aynı job ID'li verified private manifest + checksum/permission + queued database identity ile replay etmeden job'ı kapatsın.
- [ ] `database.restore` kaynak zinciri production wiring'de hazır; gerçek restore kabulünde selected backup için same-server succeeded `database.backup` job evidence + private manifest SHA birebir eşleşmeden preview/apply açılmadığını ve queued payload'ın yalnız `databaseName + backupId + expectedBackupSha256` taşıdığını doğrula.
- [ ] Restore öncesi `pre-restore:<jobId>` private backup alınmalı; hedef schema drop edilip selected canonical dump uygulanmalı; restore sonrası canonical re-dump SHA selected backup ile birebir eşleşmeli. Restore/verification failure pre-restore dump'ı geri uygulayıp rollback re-dump SHA ile doğrulamalı; rollback de başarısızsa açık rollback-failed sonucu vermeli ve başarı raporlanmamalı.
- [ ] Restore lost-ack recovery host mutation'ını tekrar çalıştırmadan selected backup + pre-restore backup + live canonical digest + private receipt evidence ile kapanmalı; stale/tampered backup, engine drift, missing pre-restore evidence veya digest mismatch unresolved kalmalı. Büyük dump için bounded `db.restore.*` stage ve `progress=N` metadata'sı job log yüzeyinde görünmeli.
- [ ] DB public job/audit/log/recovery CLI ve private receipt state'lerinde raw SQL, socket path, client raw output veya connection materialı bulunmadığı gerçek package üzerinde doğrulansın.
