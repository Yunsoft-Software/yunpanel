# YunPanel — Gerçek Ortam / Kabul TODO

Bu dosyada yalnız bu geliştirme oturumunda güvenilir biçimde yapılamayan **gerçek Node 24, browser, Ubuntu, package, DNS ve rollback kabul işleri** tutulur. Kod geliştirme işleri `plan.md`, bağlayıcı kurallar `agents.md` içindedir. GitHub Actions kullanma. Secret/parola/cookie/MFA secret/private key veya kullanıcı verisini repo, log, screenshot ya da test fixture'ına yazma.

2026-09-09/10 tarihindeki Node24/package/canlı kabuller tarihsel referanstır; sonraki agentless recovery, migration backup/stage/validate, common audit ve kalıcı Website source değişikliklerini kapsamaz. Güncel `main` ayrıca doğrulanmadan “full check geçti” denmez.

## T-RUNTIME — P0 güncel full check

- [ ] Mixed/stale web/API build ile privileged management'in fail-closed kaldığını ayrıca doğrula.

## T-AUTH-AUDIT — P0 gerçek HTTPS/browser kabulü

- [ ] Gerçek HTTPS Owner akışında setup/login/TOTP/recovery/password/session/logout/logout-all/idle/absolute timeout çalışsın.
- [ ] İki tab browser race: delayed 200/401, cookie rotation, lost MFA response, `pageshow`, keep-alive ve session revoke yarışlarını doğrula.
- [ ] Read Only session yalnız izinli resource GET/HEAD'lerine ulaşsın; mutation/jobs/users/audit/sensitive nested endpointler 403 kalsın.
- [ ] Trusted proxy/client-IP/rate-limit spoof kabulünü gerçek reverse proxy üzerinde tamamla; forwarding header doğrudan güvenilmesin.
- [ ] Owner `/api/audit` gerçek auth/MFA sınırında çalışsın; Read Only 403. actor/resource/action/outcome/time filtreleri ve pagination gerçek browser/API isteğiyle doğrulansın.
- [ ] Owner mutation → queued job → local/legacy terminal completion ve `job-recovery` terminal handoff actor bağını aynı private auth SQLite üzerinde doğrula. `system` scheduler işleri Owner gibi görünmemeli.
- [ ] Audit tablosu/job linkleri password, cookie, CSRF, env value, MFA secret, request/response body, raw job output veya terminal output içermemeli.
- [ ] Auth DB/WAL/SHM ve audit state root-owned/service-owned doğru private izinlerle kalmalı; restart/upgrade/backup-restore sonrası audit schema tekrar açılmalı.

## T-AGENTLESS — P1 gerçek Ubuntu host kabulü

- [ ] Exact `YUNPANEL_LOCAL_SERVER_ID` + OS hostname + exclusive lock + root `yunpanel-api.service` startup/shutdown zincirini gerçek Ubuntu 24.04 hostta doğrula.
- [ ] 30s local snapshot host inventory + allowlisted systemd services + Docker + Nginx'i aynı server record'a persist etsin; snapshot/binding fault executor'ı drain edip lock'u bıraksın.
- [ ] Executor disk-full/read-only/lost acknowledgement senaryolarında successful host mutation'ı failed diye yeniden yazmasın; ambiguous state yeni claim/retry'ı durdursun.
- [ ] `.recovery.json` sidecar 0600 ve secret-free kalsın; running/terminal/mixed recovery sınıflandırması restart sonrası korunsun.
- [ ] Bütün güncel recovery komutlarını gerçek host evidence/receipt ile doğrula: read-only inspect, domain stage/activate, static deploy/rollback, Node deploy/restart/rollback/process enable-disable-start-stop/runtime install, DB create/delete, service control/install/restart, system upgrade, certificate issue/renew.
- [ ] Generic force-success/force-failed, blind mutation retry veya evidence-free journal clear yolu bulunmasın.
- [ ] Node/static clone/install/build ve runtime dedicated `yunapp-*`; Node systemd unit `NoNewPrivileges=true`, boş capabilities ve bounded writable path kullansın.
- [ ] Local ownership altında retained legacy heartbeat/command/environment/result 409 `server_managed_locally` kalsın.
- [ ] Web service process environment/state mountlarında control-plane secrets görünmesin.

## T-MIGRATION — P1 agentless migration/rollback

- [ ] Gerçek packaged hostta API+agent stopped + queue/recovery clear iken `local-migration-backup create -> verify -> preview -> stage` zincirini çalıştır.
- [ ] Backup/snapshot/stage rootları 0700, archive/manifest 0600; checksum tamper, missing required path, source symlink, duplicate/special member, link escape, type drift ve partial-stage cleanup fail-closed olsun.
- [ ] `/etc/passwd` ve `/etc/group` yalnız `yunapp-*` identity reference olsun; live overwrite yapılmasın. UID/GID/home/shell/group drift doğru raporlansın.
- [ ] Enrolled host: verified snapshot sonrası `bind <id> --backup-dir <snapshot> --confirm`, exact env ID, agent disable, API start, `local-runtime validate <id>` sırasını uygula.
- [ ] Fresh agentless host: verified snapshot sonrası `create --backup-dir <snapshot> --confirm`; `agentTokenHash=null`, enrollment credential yok. API start sonrası `validate` geçmeden functional mutation yapma.
- [ ] `validate` exact local ownership/hostname, API active, agent inactive, queue/recovery zero, fresh local snapshot, exact packaged API version ve loopback `/api/health` 200 `status=ok` istemeli.
- [ ] Existing enrolled host rollback: new rollback snapshot → verify/preview/stage → API+agent stop → `release <id> --backup-dir <snapshot> --confirm` → agent enable/start. Fresh local-only identity release edilememeli.
- [ ] Live restore/apply kodu açıldığında per-target replace, owner/mode/ACL/xattr, pre-apply backup, health gate ve deterministic rollback gerçek isolated hostta kanıtlanmadan production'a alınmasın.
- [ ] Migration + rollback kabulü tamamlandıktan sonra `yun-agent.service` ve retained transport kaldırılırken package upgrade disabled agent'ı tekrar enable etmemeli.

## T-WEBSITE — P1 kalıcı Website/domain kabulü

- [ ] Production package `YUNPANEL_WEBSITE_STORE=/var/lib/yunpanel/control-plane/website-registry.json`, `YUNPANEL_WEBSITE_MIGRATION_POLICY_STORE=/var/lib/yunpanel/control-plane/website-migration-policy.json`, `YUNPANEL_DNS_HOSTING_STORE=/var/lib/yunpanel/control-plane/dns-hosting-registry.json`, `YUNPANEL_MAIL_DOMAIN_STORE=/var/lib/yunpanel/control-plane/mail-domain-registry.json` ve `YUNPANEL_DOCKER_WORKLOAD_STORE=/var/lib/yunpanel/control-plane/docker-workload-registry.json` altında private state oluştursun; restart/upgrade kimlikleri, explicit ilişkileri veya policy mode/digest bilgisini değiştirmesin.
- [ ] Authenticated API'de Owner Website create/list/detail; Read Only list/detail/explicit Website→Domain read ve POST/migration 403 davranışını gerçek listener üzerinden doğrula.
- [ ] Website store v1/v2→v3 upgrade'i gerçek package state üzerinde ID değiştirmeden revision/proxy target/null Docker-workload alanlarını eklesin. Owner update preview/apply exact revision + linked-Domain digest + typed confirmation istesin; stale Website/Domain state ve duplicate Application/Docker rebind mutation üretmesin, Read Only iki update route'unda 403 kalsın.
- [ ] Static/Node Website binding'i canonical application root + deterministic `yunapp-*` user kullansın; stale/missing/cross-server application reference startup'ta fail-closed olsun.
- [ ] Domain `websiteId` ilişkisi same-server ve existing Website şartını korusun; legacy kayıtlar compatibility modda `websiteId=null` olarak okunabilsin ve otomatik server/port/hostname tahminiyle kalıcı bağ oluşturulmasın.
- [ ] IDN hostname inputları gerçek API'de canonical ASCII punycode olarak persist olsun; Unicode/punycode eşdeğerleri duplicate hostname/alias olarak kabul edilsin.
- [ ] Owner site-create preview/apply'i gerçek packaged listener'da static, Node, existing Application, explicit external/unverified Docker workload ve external proxy ile doğrula. Exact operation/digest/confirmation, managed port collision, Docker same-server/unique binding, explicit `www` alias/child, stale-state no-mutation ve Application/Website sonrası kesilen işlemin duplicate üretmeden devamı korunmalı; DNS/certificate/mail/container sonucu false kalmalı.
- [ ] Owner DNS-zone/mail-domain external tracking create/list/detail akışını gerçek packaged listener'da doğrula. Kayıtlar `unverified` başlamalı, explicit nullable web Domain ilişkisi exact canonical hostname istemeli, side-effect sonucu false kalmalı, Read Only yalnız GET yapabilmeli ve site-create bu store'ları örtülü mutate etmemeli. Mail config preview canonical Postfix candidate map/hash üretmeli fakat external lifecycle için `readyToApply=false`, typed blocker ve `sideEffects=false` kalmalı. Restart sonrası revision/observation metadata'sı korunmalı; public API sahte `ready` yazamamalı.
- [ ] Owner Domain reparent preview/apply gerçek listener'da exact explicit parent/digest/typed confirmation istesin; stale hierarchy, cycle, cross-server ve dot-boundary ihlali mutation üretmesin. Reparent sonrası hostname, target, certificate ve desired/staged/applied traffic state aynı kalsın; Read Only iki POST route'unda 403 alsın.
- [ ] Owner Website/Domain move-delete impact preview'ını gerçek packaged listener'da doğrula: child/linked Domain, Application, explicit Docker workload/DNS-zone/mail-domain, certificate ve queued/running job kimlikleri bounded olsun; mailbox/backup/cron association yokken unavailable blocker ve `safeToApply=false` dönsün. Read Only iki POST route'unda 403 kalsın; apply/cascade rotası bulunmasın.
- [ ] Owner `GET /api/websites/migration/preview` ve `/status` için deterministic digest/current policy üretimini doğrula. Read Only her iki migration route'unda 403 kalmalı.
- [ ] Existing-Website migration bind exact `domainId + websiteId + previewDigest + typed confirmation` istemeli; state değişirse stale digest hiçbir Domain mutationı üretmemeli, aynı tamamlanmış bind retry'ı idempotent kalmalı.
- [ ] Migration finalize yalnız fresh preview bütün Domainleri `already_bound` gösterdiğinde exact digest ile `compatibility -> enforced` geçsin. Enforced modda yeni unbound managed Domain 409 `website_binding_required`, same-server explicit Website ile create başarılı olsun.
- [ ] Policy rollback yalnız exact enforced digest + typed confirmation ile compatibility moduna dönsün; migration-only binding rollback exact ledger identity/digest istesin; iki rollback de Nginx target, certificate, application release veya Domain traffic revision değiştirmesin.
- [ ] Apex + bağımsız subdomain + alias + application + certificate + Website-create/bind orchestration + policy finalize/rollback'i gerçek test domainiyle uçtan uca doğrula.

## T-SERVICES-DB — P1/P2 gerçek host functionality

- [ ] Node configuration preview/apply'i packaged Owner API'de runtime/startup/package-manager/mode/document-root değişiklikleriyle doğrula. Exact revision/digest/confirmation, immutable managed port, queued-job conflict ve pending desired config sırasında restart/status'un `activeRuntime` kullanması korunmalı; npm/pnpm/yarn işlemleri exact site user ve release-contained cwd ile çalışmalı.
- [ ] Node process enable/disable/start/stop'u packaged Owner API'de exact release-bound confirmation ve `activeRuntime` ile doğrula. Start health failure servisi stopped duruma döndürmeli; API kesintisi sonrası `recover-node-process` yalnız exact private intent + gerçek systemd/health final-state kanıtıyla job'ı kapatmalı ve kör komut tekrarı yapmamalı.
- [ ] Node 22/24 inventory ve eksik runtime install'ı packaged Ubuntu hostta doğrula: resmi `latest-v<major>.x` HTTPS manifest checksum'ı eşleşmeli, x64/arm64 dışı host ve bozuk mevcut runtime fail-closed kalmalı, `/opt/yunpanel/node-runtimes/v<major>` atomik oluşmalı. Node 20/EOL install reddedilmeli; `/usr/local/bin/node` panel version/path aynı kalmalı; deploy/npm lifecycle/systemd PATH exact seçilen managed major'ı kullanmalı. Kesinti sonrası `recover-node-runtime-install` kurulum komutunu tekrarlamadan host inventory kanıtıyla job'ı kapatmalı.
- [ ] Static ve Node deploy'u gerçek GitHub branch, annotated/lightweight tag ve full commit SHA ile doğrula. Fetch/checkout/install/build exact `yunapp-*` kullanıcısıyla çalışmalı; olmayan ref ve commit mismatch mevcut release'i değiştirmeden fail-closed kalmalı, seçilen hedef ile çözülen SHA release geçmişinde ayrı görünmeli.
- [ ] Packaged HTTPS panelde gerçek GitHub webhook'unu static ve Node Application için doğrula. Owner secret CRUD yalnız masked metadata göstermeli; exact public `/api/webhooks/github/<application-id>` panel IP allowlist'i dışında erişilebilirken diğer API yolları kapalı kalmalı. Doğru SHA-256 imzalı configured-branch push full commit deploy job'u üretmeli; yanlış imza/repo, oversize/bozuk payload ve cookie/Origin/Authorization fail-closed kalmalı. Aynı delivery servis restartından sonra aynı job'a dönmeli, farklı eşzamanlı delivery 409 resource lock almalı ve secret/delivery UUID public job, audit veya loglarda görünmemeli.
- [ ] Private GitHub repository deploy'unu read-only fine-grained token ve repository-scoped SSH deploy key ile packaged hostta doğrula. Token URL/argv/job/recovery/audit/build env'e girmemeli; SSH key `0600` site-user temporary file olup fetch sonrası silinmeli. `/etc/ssh/ssh_known_hosts` gerçek GitHub host key'iyle önceden doğrulanmış olmalı; missing/mismatch halinde deploy mevcut release'i değiştirmeden fail-closed kalmalı. Credential master-key rotation + rollback sonrasında yeniden materialize edilebilmeli.
- [ ] Application `.env` merge/replace importunu packaged Owner API ve UI'da doğrula. Duplicate/reserved/multiline/oversize ve stale revision hiçbir mutation üretmemeli; replace exact confirmation istemeli. Kaydedilen revision restart/deploy öncesi `saved_on_disk`, exact revision sağlıklı Node prosese uygulandıktan sonra `applied_to_running_process` olmalı; queued/running iş sırasında env/credential değişimi 409 kalmalı ve secret plaintext job/result/audit'e girmemeli.
- [ ] Owner-only log backend'ini packaged local Ubuntu hostta doğrula: Node journal, allowlist'li systemd/Nginx unit, current Nginx access/error no-follow dosyaları ve gerçek static/Node Git+dependency+build çıktısı okunmalı. Zaman/seviye/arama/cursor, finite NDJSON ve text download limitleri çalışmalı; log rotation stale cursor'ı fail-closed bırakmalı. Token/JWT/private key/Authorization/env secret örnekleri diskte ve yanıtta redacted, job log dizini `0700`, dosyalar `0600`, Read Only/remote host/keyfi unit-path 403/409/404 kalmalı; 30 gün/500 job ve job başına 1.000 entry/512 KiB retention gerçek dosyalarla kanıtlanmalı.
- [ ] Nginx, MariaDB/MySQL, Docker, Cron, Postfix, Dovecot, Rspamd inspect ve güvenli bir servis üzerinde install/start/stop/restart gerçek `apt/systemctl` ile çalışsın. Postfix `postfix check`, Dovecot `doveconf -n` ve Rspamd `rspamadm configtest` sonucu yalnız bounded health state üretmeli; komut çıktısı API/job'a girmemeli.
- [ ] Ubuntu 24.04 Dovecot üzerinde YunPanel'in canonical unpadded Argon2id PHC formatını geçici test mailbox'ıyla doğrula. Managed `10-auth.conf` dağıtım PAM include'una fallback bırakmamalı; Maildir yolu, lowercase tam adres, Postfix-owned `0600` LMTP socket ve managed-domain postmaster ayarı `doveconf -n` çıktısında exact görünmeli. Protected passwd-file `0600`/root kontrollü kalmalı; public preview yalnız digest/boyut/adet göstermeli, hash veya rendered satır API/job/audit/log'a girmemeli. TLS materyali olmadan plaintext auth açılamamalı ve gerçek auth testi başarılı olmadan mailbox apply açılmamalı.
- [ ] Ubuntu 24.04 Rspamd/Postfix üzerinde loopback-only `127.0.0.1:11332` self-scan Milter entegrasyonunu doğrula. SMTP ve non-SMTP Milter parametreleri protocol 6 kullanmalı; scanner durduğunda `tempfail` ile mail ertelenmeli ve unscanned kabul olmamalı. `rspamadm configtest`, `postfix check`, reload sonrası socket/service health ve GTUBE post-condition kanıtlanmadan apply başarılı sayılmamalı.
- [ ] Packaged Owner mail preview'ında aggregate bundle digest/order ve domain/mailbox/alias adetlerini doğrula. Postfix recipient seti ile Dovecot account seti farklıysa preview fail-closed kalmalı; `mydestination` çakışması, relay policy, TLS, Unix identity veya loopback Milter portu kanıtlanmadığında `readyToApply=false` olmalı. Protected passwd içeriği/hash public preview, job, audit veya log'a girmemeli.
- [ ] Ubuntu 24.04 üzerinde Roundcube `roundcube-core` detection/install ve sabit entrypoint/config/PHP syntax kontrollerini doğrula. Başarılı paket/config kanıtı `active=false`, `units=[]`, `health.status=installed` kalmalı; systemd control işi kuyruğa alınmamalı ve bu sonuç web endpointi/veritabanı/mailbox hazırlığı sayılmamalı.
- [ ] MariaDB↔MySQL conflict fail-closed kalsın; mevcut DB engine bozularak değiştirilmesin.
- [ ] MySQL/MariaDB Unix socket root auth ile engine/version/non-system inventory; test DB create→inspect→drop→inspect çalışsın. System DB ve injection isimleri reddedilsin.
- [ ] DB/job/private receipt state'inde raw SQL/socket path/client output/credential bulunmasın.

## T-PACKAGE-LIVE — P0/P1 package ve canlı kapı

- [ ] Matching Ubuntu `amd64`/`arm64` build hostunda approved `node-pty@1.1.0` install scriptiyle temiz `npm ci` çalıştır; native `pty.node` ve `spawn-helper` yüklenebilmeli. Üretilen `.deb` `all` değil doğru mimariyi bildirmeli, clean install/upgrade sonrasında `/usr/local/bin/node` ile `node-pty` importu ve gerçek PTY spawn çalışmalı; farklı OS/mimari veya eksik native build paketleme öncesi fail-closed kalmalı.
- [ ] Node24 full check tamamlandıktan sonra yeni `.deb` üret; `dpkg-deb -c/-I` ile root API, Website/audit/migration-policy files, local-runtime, migration backup CLI, bütün job-recovery komutları, receipts, runbooklar ve web sandbox'ın aynı committen paketlendiğini doğrula.
- [ ] İzole Ubuntu hostta clean install + old-package upgrade yap. Auth DB/master key/state permissions, disabled-agent preservation, Website/audit/migration-policy schema ve local runtime startup davranışı korunmalı.
- [ ] Hosted static/Node siteler panel restart/upgrade sırasında çalışmaya devam etsin.
- [ ] `0.3.0-4` ↔ yeni agentless aday package/state rollback provası yap; IDs, auth, master key, vhost, cert, Website/application/domain/policy ve release state korunmalı.
- [ ] Gerçek live hosta ancak isolated package/migration/rollback kabulünden sonra geç.

## T-FEATURE-ACCEPTANCE — gerçek servis/provider/browser kabulü

- [ ] Packaged Owner API'de Domain canonical-hostname/alias/managed-HTTPS/HTTP→HTTPS/canonical-redirect preview+apply akışını gerçek Nginx ve test DNS'iyle doğrula. Exact digest+typed confirmation, IDN canonicalization, conflict/hierarchy/no-op/stale preview ve queued/running Domain/certificate işi hiçbir mutation üretmemeli. Canonical hostname değişiminde eski Nginx dosyası başarılı reload sonrası kalkmalı; `nginx -t`, replace veya reload arızası iki filename'ı önceki çalışan duruma döndürmeli ve güvenli actionable diagnosis vermeli. HTTP/HTTPS curl matrisi ACME location, alias URI preservation ve redirect-disabled serving'i kanıtlamalı. Hostname-set değişiminde eski certificate detach olmalı; başarılı replacement sonrası `superseded` kalıp renewal'a girmemeli, stale issuance yeni Domain'e bağlanmamalı ve private key/job/audit'e çıkmamalı.
- [ ] Aynı packaged Domain preview/apply→stage→activate hattında proxy için upload size/timeout/WebSocket/header; static için upload size/SPA fallback/cache/header matrisini gerçek Nginx ile doğrula. Response header inheritance, cache disabled/null/custom seconds, redirect server-blockları ve upper/lower bounds beklenen `curl` davranışını vermeli. Raw directive/header injection, target-type mismatch ve stale preview live config'i değiştirmemeli; `nginx -t`/reload arızası önceki çalışan checksum'a dönmeli. Domain store v1/v2→v3 geçişi eski target WebSocket/SPA ve aktif traffic revision'ını korumalı.
- [ ] Packaged API'de kasıtlı DNS provider, Certbot, certificate material, `nginx -t`, reload ve rollback arızaları üret; public certificate/list/job/command-result/audit çıktılarının PEM, email, material digest, certificate/fullchain/private-key path, child-process output ve hostile error code içermediğini doğrula. Her hata yalnız authored bounded message/action vermeli; unknown kod scope-specific generic teşhise düşmeli. Protected store/recovery ise exact certificate path identity ile başarıyla reconcile edebilmeli.
- [ ] Packaged root API'de Owner custom certificate preview/import ve existing certificate selection akışını gerçek RSA/EC materialle doğrula. Exact digest+typed confirmation, current hostname/SAN/wildcard coverage, validity, leaf/fullchain, certificate-private-key match, stale/concurrent request ve local-server sınırı fail-closed kalmalı. Material `/var/lib/yunpanel/control-plane/custom-certificates/<uuid>` altında `0700/0600` olmalı; PEM/private key API, public job, audit, log veya web process environment'ına çıkmamalı. Import/select sonrası Domain restage + `nginx -t` + activate ve eski certificate `superseded` davranışı gerçek HTTPS curl ile kanıtlanmalı; diskte key değiştirilirse stage reddedilmeli. Restart/upgrade/backup-restore certificate registry v3 kimliğini ve materialı korumalı.
- [ ] Ubuntu 24.04 test hostunda packaged `python3-certbot-dns-cloudflare` ile gerçek Cloudflare zone/token kullanarak DNS-01 staging→production→automatic/manual renewal zincirini doğrula. Exact-name ve apex+`*.zone` SAN'ları, deeper hostname'in explicit SAN kalması, TXT propagation/cleanup, least-privilege token yetkisi ve credential revoke/delete hatası fail-closed olmalı. Token API/public job/audit/log/argv/web process environment'ına çıkmamalı; geçici Certbot dosyası/dizini `0600/0700` olup success/failure sonrası silinmeli. Restart/upgrade ve master-key rotate→rollback sonrasında DNS credential store ile certificate registry v2→v3 kimliği/challenge bağı korunmalı; Domain restage+`nginx -t`+activate sonrası gerçek wildcard HTTPS curl çalışmalı.
- [ ] Packaged HTTPS panelde gerçek Chromium/Firefox ile Owner root ve static/Node `yunapp-*` site terminalini doğrula: exact user/cwd, Ctrl+C/Ctrl+D, Unicode/IME, `vim`/`top` gibi fullscreen TUI, resize, beş paralel session ve kapatıp yeni PTY'ye yeniden bağlanma çalışmalı. Yanlış Origin/cookie/MFA/role/capability, replay ve URL token reddedilmeli; logout/logout-all/session delete/expiry, password/MFA/role/user disable-delete açık socketi ve process group'u derhal kapatmalı. Idle/absolute/output/backpressure limitleri orphan bırakmamalı; audit yalnız open/close metadata içermeli, keystroke/output/history içermemeli.
- [ ] Ubuntu paketinde static ve Node Website file API'sini gerçek `yunapp-*` hesaplarıyla doğrula: aktif UUID release dışında erişim, absolute/traversal/ara-final symlink okuma-yazma-rename ve siteler arası kaçış reddedilmeli; symlink'in kendisi confirmed delete ile hedefe dokunmadan silinmeli. List permission/uid/gid, UTF-8/SHA-256 stale edit, atomic binary create/replace/download, mkdir/rename, 512 KiB/16 MiB/1.000-entry limitleri ve exact delete confirmation çalışmalı. Yeni static deploy site-user ownership'ünde kalırken Nginx sunumu bozulmamalı; eski root-owned static release için güvenli redeploy geçişi belgelenmeli. Read Only/remote/proxy/Docker hedefleri reddedilmeli ve audit path/içerik taşımamalı.
- [ ] Packaged Owner API'de DNS readiness refresh'i gerçek public resolver ve dual-stack host fixture'ıyla doğrula. Canonical A/AAAA ve CNAME→adres çözümü, eksik/mismatched kayıt, resolver timeout/arıza, inventory adresi yokluğu, Domain active-revision şartı ve Cloudflare credential var/yok/okunamıyor ayrımı bounded reason/action üretmeli. Yalnız exact `expectedRevision` güncel lifecycle durumunu yazabilmeli; stale istek mutation yapmamalı, Read Only POST yapamamalı ve resolver/provider hataları private ayrıntı taşımamalı.
- [ ] Packaged Owner API'de gerçek least-privilege Cloudflare token/zone ile A/AAAA/CNAME create-update-delete ve exact no-op tekrarını doğrula. Preview digest provider snapshot/zone revision/credential revision değişiminde stale kalmalı; TTL/proxy, zone-boundary, duplicate record, unauthorized/rate-limit/timeout/provider error durumları mutation veya secret sızıntısı üretmemeli. İş yalnız local `dns_zone` durable job'ıyla çalışmalı; token API/public job/recovery/audit/log/URL'ye çıkmamalı. API+agent duruyken `recover-dns-record` exact private intent ve idempotent provider post-condition ile uncertain create/update/delete sonucunu çözmeli; kanıtlanamayan durum running kalmalı. Başarılı mutation otomatik propagation/HTTP-01 hazır sonucu sayılmamalı, readiness ayrıca gerçek resolver ile yenilenmeli.
- [ ] Mail, Docker, backup, cron ve Plesk importer ilgili `plan.md` kodu tamamlandıkça gerçek provider/host fixture'larında doğrulanır.
- [ ] Görsel UI/UX responsive/polish kabulü backend functionality tamamlandıktan ve tasarım ayrı modele verildikten sonra yapılır.

## Yayın kuralı

Repoda kod bulunması kabulün geçtiği anlamına gelmez. Güncel Node24/full workspace, gerçek browser/HTTPS, package/Ubuntu ve ilgili DNS/mail/Plesk/migration testleri tamamlanmadan production-ready etiketi verme. GitHub Actions kullanma.
