# YunPanel — Gerçek Ortam / Kabul TODO

Bu dosyada yalnız kaynak testleriyle güvenilir biçimde tamamlanamayacak gerçek Ubuntu, package, browser, DNS/provider, mail delivery, storage ve rollback kabulleri tutulur. Kod işleri `plan.md`, hedef mimari `docs/architecture.md`, bağlayıcı kurallar `agents.md` içindedir.

IP adresi `.44` ile biten Plesk sunucusu kesinlikle kapsam dışıdır. Bütün SSH/package/deploy testleri yalnız repo dışı `.local/test-server.env` içindeki açık YunPanel test sunucusunda, hedef adresin `.44` olmadığı doğrulandıktan sonra yapılır. Secret/parola/cookie/MFA/private key ekran görüntüsü, rapor, log veya repoya yazılmaz.

## T-BASE — P0 güncel güvenlik ve package kapısı

- [ ] Clean install ve önceki paketten upgrade; auth DB/master key, Domain/Website/Application kimlikleri, release'ler, Nginx/certificate, mail/database state'i ve root-owned private izinleri korusun.
- [ ] Production exact `YUNPANEL_LOCAL_SERVER_ID` + OS hostname olmadan başlamasın; uzak/eski server detail/mutation ve retained agent transport fail-closed kalsın (NODE_ENV=production altında boş ID ile startup abort, kayıt dışı UUID ile local_server_not_found fail-closed, assertBoundServer hostname/executionMode kısıtı 2026-09-20'de .28 üzerinde doğrulandı; `docs/history/local-server-identity-fail-closed-live-acceptance-2026-09-20.md`).
- [ ] `.28` geliştirme hostunda `YUNPANEL_OWNER_MFA_REQUIRED=false` kalırken gerçek HTTPS Owner password-only setup/login/session/logout/logout-all/idle/absolute timeout, CSRF, rate-limit ve trusted-proxy sınırı Chromium/Firefox'ta çalışsın. TOTP/recovery bu hostun geliştirme kabul kapısı değildir; kullanıcı açıkça tersini istemedikçe `.28` üzerinde MFA etkinleştirilmez veya kodu istenmez.


## T-RUNTIME — P0 Passenger, PHP-FPM ve hosted app sürekliliği

- [ ] Passenger, PHP ve Python Website; yeni Debian paketi upgrade'i sırasında hizmet vermeye devam etsin (.28 üzerinde panel API/web restartı sırasında HTTP 200 ve gövde SHA-256 sürekliliği Passenger, PHP ve Python için 2026-09-20'de doğrulandı).

## T-TOOLS — P0 ttyd, elFinder, phpMyAdmin/pgAdmin gateway

- [ ] elFinder/ttyd replacement kabulü tamamlanınca özel site-file-manager ve node-pty/xterm yollarının kaldırılması upgrade/rollback ile denenip açık session/orphan bırakmadığı doğrulansın (ttyd 1.7.4 root sunucu ve site kullanıcısı one-shot oturumları, elFinder 2.1.70 connector ve site başına izole PHP-FPM havuz materyalizasyonu/telafisi 2026-09-20'de `.28` üzerinde doğrulandı; `docs/history/ttyd-elfinder-tools-gateway-live-acceptance-2026-09-20.md`. Özel `site-file-manager` ve `node-pty`/xterm kaynak koddan tamamen kaldırıldı).
- [ ] PostgreSQL/pgAdmin açıldığında aynı Website role/scope, gateway auth ve çapraz-site reddi gerçek PostgreSQL ile doğrulansın.

## T-DNS — P0 PowerDNS ve nameserver

- [ ] Authoritative DNS retirement'ı yalnız P0.9 Domain delete orchestrator'ı içinden gerçek PowerDNS'te doğrula: durable provisioning history exact `created=true` origin evidence'ını çözsün; current Website binding kaldırılmış olsa da server/webDomain/zone history kaybolmasın. Manual RRset, DNSSEC, local mail-domain, active Domain job, certificate/routing/child dependency ve ownership belirsizliği destructive DELETE'i bloklasın. `YUNPANEL_DNS_ZONE_SNAPSHOT_RETENTION_DAYS` yoksa confirmation üretilmesin; 1..3650 dışı startup'ta reddedilsin.
- [ ] DNS retirement private snapshot journal gerçek hostta root-private `0700/0600` kalsın ve public operation response RRset content/original confirmation taşımamalı. PowerDNS DELETE öncesi live zone retained snapshot ile exact eşleşsin; RRset/content/comment/kind/DNSSEC drift'i DELETE göndermeden fail-closed kalsın. DELETE lost-ack/process-kill sonrasında restart zone absent ise ikinci DELETE atmadan `deleted` kapanabilsin; zone exact snapshot olarak duruyorsa automatic replay yerine typed retry istesin. `retainUntil` configured snapshot retention süresiyle birebir eşleşsin ve upgrade/restart boyunca snapshot digest korunmalı.
- [ ] Çalışan managed PowerDNS config varken invalid candidate enjekte et; configtest failure sonrası dosya byte-for-byte last-good içeriğe ve aynı uid/gid/mode'a atomik dönsün, servis sağlıklı state'ini kaybetmesin (.28 üzerinde geçersiz aday testi pdns_server --config=check ile doğrulandı, 0640 ve byte-for-byte içerik korundu; 2026-09-20, `docs/history/powerdns-socket-health-authoritative-live-acceptance-2026-09-20.md`).
- [ ] Fresh-install fixture'da previous managed config yokken invalid candidate enjekte et; rejected candidate aktif managed path'te bırakılmasın ve sonraki clean apply eski bozuk dosyadan etkilenmesin.
- [ ] Durable PowerDNS apply'i host mutation boundary sonrasında process/API kill veya timeout ile kes; restart/retry önce `inspect` yapsın ve underlying package/config/service mutation sayısı duplicate replay nedeniyle artmasın. Hedef state kanıtlanabiliyorsa journal `succeeded` kapanabilsin; kanıtlanamıyorsa recovery-pending/fail-closed kalsın ve explicit operator kararı olmadan mutation tekrarlanmasın. Pre-mutation rollback snapshot'ı gerçek hostta root-owned `0600` olsun, restart sonrasında aynı operation/digest ile okunsun, retry sırasında değişmesin ve symlink/tamper state'i mutation başlamadan reddedilsin.
- [ ] PowerDNS explicit rollback gerçek hostta previous config + authoritative receipt'i byte/uid/gid/mode olarak restore etsin; configtest/restart/API health sonrası exact previous topology doğrulansın. Verified-current compensation snapshot'ı root-owned `0600` kalsın. Process config/receipt replacement'ın iki olası sırası arasında öldürüldüğünde restart exact previous/current bytes dışındaki state'i ezmeden mixed state'i previous hedefe tamamlasın. Rollback API veya UDP/TCP/recursion health failure enjekte edildiğinde current config/receipt geri gelsin ve current intent socket policy ile yeniden healthy olsun; health kanıtı oluşmadan journal `rolled_back` kapanmasın, compensation failure başarılı rollback gibi raporlanmasın.
- [ ] Encrypted API-key store/restart/rotation provasında raw key browser/API/audit/job/log/process argv/config içinde görünmesin; `pdns.conf` yalnız `pdnsutil hash-password` çıktısını tutsun (`pdns.d/yunpanel.conf` içinde yalnız `$scrypt$` tutulduğu ve 0640 korunduğu 2026-09-20'de `.28` üzerinde doğrulandı).
- [ ] Local readiness gerçek hostta hem UDP/53 hem TCP/53 loopback probe'u geçsin; dış bir resolver/hosttan public UDP ve TCP 53 erişimi ayrıca doğrulansın ve firewall/NAT engeli local health'i public-ready yapmasın (hem yerel hem dış UDP/TCP 53 sorguları ve yetkili qr/aa bayrakları 2026-09-20'de `.28` üzerinde doğrulandı; `docs/history/powerdns-socket-health-authoritative-live-acceptance-2026-09-20.md`).
- [ ] Recursive probe gerçek PowerDNS'te RA=false ve REFUSED ile dönsün; `pdns-recursor` kurulu fixture apply'i fail-closed bloklasın ve YunPanel mevcut recursor'ı sessizce kaldırmasın/değiştirmesin (yerel ve dış özyinelemeli sorgularda RA=false ve REFUSED 2026-09-20'de `.28` üzerinde doğrulandı; `docs/history/powerdns-socket-health-authoritative-live-acceptance-2026-09-20.md`).
- [ ] Zone/RRset create-update-delete/no-op; SOA serial, NS, A/AAAA/CNAME/MX/TXT/CAA/SRV, TTL, wildcard, IDN ve DNSSEC API üzerinden gerçek authoritative cevapla doğrulansın. Yeni Website provisioning compensation'ında yalnız persisted `created=true` evidence + exact canlı managed RRset intent'i zone DELETE açsın; pre-existing/no-change zone korunsun, pre-existing değişiklik/evidence kaybı fail-closed kalsın ve create/PATCH hatası sırasında eklenen manual RRset kör cleanup ile silinmesin. Enabled local-mail zone re-apply shared mail identity MX, IMAP/submission SRV ve current+pending-retirement DKIM TXT kayıtlarını authoritative cevapta üretsin; disabled local-mail re-apply yalnız YunPanel `mail` ownership kayıtlarını kaldırsın, manual kayıtları korusun ve webmail/autodiscover endpoint'i hazır değilken dead kayıt yayınlamasın. Local DKIM rotation retirement failure-injection: intent persist, provider mutation ve retirement clear sınırlarında process öldürüldüğünde restart/retry selector'ı geri yayınlamadan veya duplicate mutation üretmeden authoritative yokluk kanıtıyla kapansın. Manual/mismatched selector ve unrelated zone drift fail-closed kalsın.
- [ ] Pre-existing local zone için durable re-apply/rollback'ı gerçek PowerDNS'te failure-injection ile doğrula: operation mutation öncesi exact normalized before snapshot ile deterministic expected-after snapshot/digest'i root-private journal'a yazsın; pre-existing manual RRset/content/comment/kind/DNSSEC evidence'ı korunsun. PATCH cevabı öncesi/sonrası process kesildiğinde exact after-state ikinci mutation olmadan `succeeded` kapanabilsin; yalnız journaled before/after parçalarından oluşan mixed state `dns_zone_reapply_partial_apply_detected` ile failed-but-rollbackable kalsın; foreign/manual üçüncü state rollback'i fail-closed bloklasın. Typed rollback operation ID + monoton `updatedAt` + before/after digest'e bağlı olsun, zone DELETE/recreate etmeden yalnız operation-owned RRset/kind geçişlerini geri alsın ve pre-existing manual kayıtları korusun. `rolling_back` sırasında process kesilirse restart yalnız inspect etsin, host mutation'ı otomatik replay etmesin; exact before kanıtlanırsa kapanabilsin, aksi halde explicit retry gerektirsin.
- [ ] New Website local-DNS provisioning'i apex/www/mail/webmail kayıtlarını seçilen policy'ye göre oluştursun; external-DNS Website'e örtülü PowerDNS zone eklemesin.
- [ ] En az iki bağımsız authoritative endpoint veya onaylı secondary DNS ile delegation/transfer/failover testi yap. Tek host iki NS adıyla healthy gösterilmesin; glue/parent delegation eksikliği actionable kalsın.
- [ ] DNSSEC enable gerçek cryptokey/DNSKEY/DS üretsin; parent DS yokken `pending_parent_ds`, matching DS registrar/parent'ta yayınlanınca `secure_ready`, yanlış/eski DS'de `parent_ds_mismatch` kalsın. `dnssec=true` fakat key/DS signing evidence eksik state `signing_material_incomplete` olsun ve successful operation sayılmasın. Parent DS lookup timeout/SERVFAIL/NXDOMAIN durumları `absent` sayılmasın. Disable yalnız parent DS gerçekten kaldırılıp public resolverda verifiably absent olduktan sonra açılsın; preview sonrası DS geri ekleme yarışı mutation'ı bloklasın. Enable ve disable için PowerDNS mutation sonrası API/process öldürülerek durable operation restart recovery test edilsin: hedef state zaten oluşmuşsa ikinci signing mutation gönderilmesin; disable sonrası parent DS yeniden görünürse operation unsafe failure, parent DS unverifiable ise `applying` kalsın. Rollover'da collection-only public cryptokey inspect ve digest-bound create/state/delete gerçek PowerDNS API ile doğrulansın; create/publish/activate/DELETE cevabı, HTTP `202` cevabı, parent DS TTL beklemesi ve rectify sınırlarında process öldürülünce root-private v4 operation store'dan restart duplicate key veya erken silme üretmesin, private key/confirmation/API key hiçbir public/job/audit/log yüzeyine çıkmasın. New-key publication/activation, exact publication SOA serial + DNSKEY'in primary/bütün secondary uçlarda görülmesi ve gözlenen DNSKEY TTL bekleme süresi, foreign DS kabul etmeyen old+new double-DS geçişi, ardından yalnız new-key DS kalan old-DS retirement + parent RRset TTL bekleme süresi ve old-key deletion gerçek resolver ile kesintisiz doğrulansın; bekleme sırasında old DS geri eklenirse tam TTL'nin yeniden başladığı kanıtlansın. Tarayıcı reload sonrasında continuation preview/POST yeni revision confirmation ile aynı operation'ı ilerletsin; eski confirmation stale kalsın ve unchanged parent/TTL state açık `waiting` dönsün.
- [ ] Mevcut Cloudflare record ve Certbot DNS-01 akışı external-DNS modunda least-privilege tokenla çalışsın; PowerDNS state'iyle karışmasın; token hiçbir public yüzeye çıkmasın.


## T-MAIL — P0 Postfix/Dovecot/Rspamd/Roundcube
- [ ] External DNS zonu ve alan adı için exact pending DNS gereksinimleri ve Cloudflare sağlayıcı uygulamasını gerçek test sunucusunda doğrula: apex A/AAAA, CNAME, mail MX/SPF/DMARC/DKIM/webmail gereksinimlerinin eksiksiz listelendiğini; Cloudflare token eklendiğinde provider canlı snapshot'ı ile pending/fulfilled eşleşmelerinin yapıldığını; tekil gereksinim anahtarı (`key`) ile önizleme ve uygulama yapıldığında `dns.record.apply` işinin hatasız kuyruğa alınıp Cloudflare üzerinde kaydı oluşturduğunu doğrula (11 kanonik gereksinimin eksiksiz listelenmesi, Cloudflare token ekleme ve maskeleme, eksik/eşleşen/uyumsuz/çakışan sağlayıcı canlı snapshot eşleşmesi, tekil anahtar ve bayat özet hata enjeksiyonları, dns.record.apply işinin 202 ile kuyruğa alınması, yerel yürütücü tarafından kaydın oluşturulması ve post-apply fulfilled doğrulaması 2026-09-21'de `.28` üzerinde tamamlandı; `docs/history/external-dns-requirements-cloudflare-live-acceptance-2026-09-21.md`).
- [ ] Shared Roundcube mapping failure-injection: bind intent persist, apply enqueue/attach, Nginx replace, reload, endpoint health, job completion ve active finalize sınırlarında process öldür. Restart mutation'ı kör replay etmesin; queued/running apply inspection-only kalsın, succeeded exact evidence yalnız metadata finalize etsin. Delete'te active→removing olduktan sonra DNS readiness hemen düşsün; shared config apply tamamlanınca removed tombstone oluşsun. Tombstone persist sonrası parent Domain removal response kaybolursa restart ikinci Roundcube apply göndermeden exact parent-owned evidence ile webmail step'ini kapatsın (Bind niyet kalıcılığı, 409 bayat özet ve hatalı onay enjeksiyonları, devam adımıyla kuyruklama, Nginx yapılandırma ve yeniden yükleme, başarı kanıtı sonrası aktif sonlandırma, silme niyetinde anında DNS hazır bulunuşluğu düşüşü, host temizliği ve removed tombstone oluşumu 2026-09-21'de `.28` üzerinde doğrulandı; `docs/history/roundcube-mapping-failure-injection-live-acceptance-2026-09-21.md`).
- [ ] İki farklı local Mail Domain mapping'i aynı shared Roundcube app/FPM/database'i kullanırken ayrı `webmail.<domain>` certificate/server-block'larıyla birlikte çalışsın. Bir Domain mapping delete diğer Domain server block'unu, shared FPM pool/socket'u, Roundcube DB/config'ini veya login'i kaldırmasın; `mailtest.webrich.news` ve `cryptoraichu.website` mapping'leri ve webmail mapping silme/disable blocker temizliği 2026-09-20'de `.28` üzerinde doğrulandı (`docs/history/site-create-local-mail-roundcube-live-acceptance-2026-09-20.md`, `docs/history/mail-domain-disable-webmail-blocker-live-acceptance-2026-09-20.md`).

- [ ] SQLite mail failure-injection: seed apply öncesi/sonrası, DB chmod/chgrp, Postfix/Dovecot config validate, reload/health, legacy lookup retirement ve durable receipt sınırlarında API/process öldür. Restart host mutation'ı kör replay etmesin; exact SQLite DB state + config + retired-legacy evidence ile tamamlanmış apply'i kapatsın, driftte fail-closed kalsın. Activation failure pre-apply v6 backup'tan hash/passwd/SQL files + DB + directory metadata'yı exact restore edebilsin. Önceki sürümden kalmış v5 backup/recovery fixture upgrade sonrasında okunup legacy digest materialization ile tamamlanabilsin (Aktivasyon hata enjeksiyonlarında v6 yedeğinden hash/passwd/SQL/DB dosyalarının ve dizin modlarının eksiksiz restorasyonu, quick_check/state digest uyumsuzluğunda geri alma, diskteki v5 yedeğinin upgrade sonrası okunup doğrulanması, eski özet somutlaştırması ile çalışan iş kurtarma ve canlı .28 sunucusunda SQLite önizleme, hata enjeksiyonu, v3 apply, v6 geri alma ve yeniden uygulama döngüsü 2026-09-21'de doğrulandı; `docs/history/sqlite-mail-failure-injection-live-acceptance-2026-09-21.md`).
- [ ] ClamAV profile açıldığında package/resource/health ve EICAR mail testi geçsin; kapalı/eksik durumda antivirus aktif gösterilmesin (kapalı/eksik durumda aktif göstermeme, sağlıksız durum raporlama ve blocker listeleme 2026-09-21'de `.28` üzerinde doğrulandı; `docs/history/mail-clamav-health-acceptance-2026-09-21.md`).


## T-BACKUP — P1 restic/rclone

- [ ] Gerçek disaster-recovery provasında boş hosta gerekli config/state/artifact restore edilip en az bir Website, database ve mailbox doğrulansın; sonuç ve tarih secret-free rapora yazılsın (Rclone remote yönetimi ve test uç noktası, Restic 0.16.4 kök-özel 0700 depo oluşturma/başlatma, bütünlük denetimi ve kilit açma, MariaDB vendor dump hook'lu Website backup seti, önizleme ve hata enjeksiyonu, anlık görüntü alımı ve geçici dizin temizliği, otomatik pre-restore anlık görüntülü ve sağlık denetimli geri yükleme ile saklama politikası prune döngüsü 2026-09-21'de `.28` üzerinde doğrulandı; `docs/history/website-backup-restore-rclone-live-acceptance-2026-09-21.md`).


## T-OBSERVABILITY-SECURITY — P1 Netdata, GoAccess, CrowdSec

- [ ] nftables ownership/reconcile gerçek hostta mevcut SSH yönetim erişimini kilitlemeden uygulanıp rollback edilsin. UFW/direct nftables çifte-yazar drift'i tespit edilip mutation bloklansın (nftables 1.0.9 lockout koruması, sözdizimi denetimi, UFW çakışma tespiti, atomik uygulama, rollback ve 0755 kalıcı konfigürasyon 2026-09-20'de .28 üzerinde doğrulandı; `docs/history/nftables-crowdsec-live-acceptance-2026-09-20.md`).
- [ ] CrowdSec engine + firewall bouncer gerçek SSH/SMTP/Nginx logunu okuyup test decision'ını nftables'a uygulasın; ban/unban, IPv4/IPv6, restart ve health görünürlüğü çalışsın (CrowdSec 1.4.6 ve crowdsec-firewall-bouncer 0.0.25 canlı log tespiti, IPv4/IPv6 ban/unban kararları ve kernel nftables set senkronizasyonu 2026-09-20'de .28 üzerinde doğrulandı; `docs/history/nftables-crowdsec-live-acceptance-2026-09-20.md`).

## T-SITE-FEATURES-SETTINGS — P1

- [ ] Site cron/systemd timer gerçek site UID/GID/cwd/env ile çalışsın; last/next run ve bounded/redacted output doğru olsun; başka Website'e veya root'a yükselmesin (Debian cron run-parts kuralı gereği noktasız dosya adı düzeltmesi, yunapp-d4c467173909 kullanıcısı, UID/GID 990, CWD /var/lib/yunpanel/data/..., sınırlandırılmış PATH/SHELL ortamı ve gerçek daemon çalıştırması 2026-09-20'de .28 üzerinde doğrulandı; `docs/history/website-cron-live-execution-acceptance-2026-09-20.md`).
- [ ] OpenSSH internal-sftp chroot/path/permission isolation iki Website hesabıyla doğrulansın; key lifecycle/materialization kabulü T-PROVISIONING altında geçsin. FTP varsayılan olarak dinlemesin (OpenSSH Match User, root 0755 chroot, /site nosuid/nodev/noexec bind mount, yalnız pubkey/sftp-only kısıtı, canlı sftp oturumu, chroot dışına çıkış engeli, kabuk reddi ve iki hesap arası çapraz erişim engeli 2026-09-20'de .28 üzerinde doğrulandı; `docs/history/openssh-internal-sftp-live-acceptance-2026-09-20.md`).
- [ ] WordPress Website'te WP-CLI ve PHP Website'te Composer yalnız site user/cwd ile çalışsın (WP-CLI 2.12.0 ve Composer 2.7.1 tespiti, runuser ile site kullanıcısı UID 990 / GID 990 düşürme, CWD sürüm dizini kısıtı, --allow-root engeli ve komut beyaz listesi 2026-09-20'de .28 üzerinde doğrulandı; `docs/history/wp-cli-composer-live-acceptance-2026-09-20.md`).
- [ ] Gerçek Chromium/Firefox ile Settings sayfasında Panel, Network/NS, Website defaults, DNS/SSL, Mail/Webmail, Databases, Backup/storage, Security, Monitoring/logs, Users/audit ve package versions alanlarının gerçek persisted state/health ile yüklendiğini, boş placeholder kalmadığını doğrula (authenticated Owner ile GET /api/panel/settings ve DNS identity/authoritative/delegation uç noktalarında gerçek state ve health 2026-09-20'de .28 üzerinde doğrulandı; `docs/history/settings-page-live-acceptance-2026-09-20.md`).

## T-DOCKER-PYTHON-MIGRATION — P2

- [ ] Managed Compose Website kendi project/network/volume/log/terminal/backup scope'uyla başka Website'ten izole çalışsın; Docker Engine/Compose dışındaki genel yönetim yeniden yazılmasın.
- [ ] Portainer adapter'ı açılırsa yalnız authenticated Owner gateway'i, local endpoint ve secret-safe session ile erişilsin; direct port public olmasın.
- [ ] Agentless migration/rollback bütün yeni vendor config/state, `yunapp-*` identity, release, PowerDNS, restic, Roundcube, tool gateway ve secrets'i korusun; ardından legacy agent fiziksel olarak kaldırılabilsin.

## T-UI — Son kabul

- [ ] Günlük navigasyonda uygulamalar yalnız ait oldukları Website altında görünsün; global Application ekranı yalnız Owner tanılama envanteri olsun.
- [ ] Website tabs runtime/capability'ye göre Hosting, Deploy, DNS, SSL, Mail/Webmail, Databases/phpMyAdmin, Files/elFinder, Logs/GoAccess, Terminal/ttyd, Backup, Cron ve Settings'i gerçek çalışır durumla göstersin.
- [ ] Gerçek Chromium/Firefox, mobil viewport, klavye ve ekran okuyucu ile deep-link/reload/back-forward, loading/error/missing dependency, modal confirmation ve uzun job progress davranışı doğrulansın.

## Yayın kuralı

Repoda adapter veya test bulunması canlı kabul anlamına gelmez. İlgili bölümün gerçek Ubuntu/package/browser/DNS/mail/storage/rollback kanıtı tamamlanmadan capability production-ready gösterilmez. GitHub Actions kullanılmaz.
